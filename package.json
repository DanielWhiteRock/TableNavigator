{
  "name": "root",
  "version": "2.0.1",
  "private": true,
  "description": "",
  "keywords": [],

  "license": "ISC",
  "author": "",
  "main": "jest.config.js",
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "build": "lerna run compile",
    "test": "lerna run test",
    "lint": "lerna run lint"
  },
  "dependencies": {
  },
  "devDependencies": {
    "lerna": "^6.4.1",
    "recursive-readdir": "^2.2.2"
  },
  "engines": {
    "node": "^16.0.0"
  }
}
